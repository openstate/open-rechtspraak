// Only required for testing in combination with jest, not used by webpack for asset compilation

module.exports = {
  presets: ['@babel/preset-env', '@babel/preset-react', '@babel/preset-typescript'],
};
